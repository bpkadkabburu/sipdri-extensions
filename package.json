{
  "name": "sipdri-extensions",
  "version": "1.0.0",
  "description": "SIPD-RI Auto Login — Chrome Extension (Manifest V3)",
  "scripts": {
    "zip": "powershell Compress-Archive -Path manifest.json,src,popup,options -DestinationPath sipdri-extension.zip -Force"
  },
  "license": "MIT"
}
